{
  "name": "bloglist",
  "version": "0.0.1",
  "description": "fullstack open 2022 part 4 exercise app",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development nodemon index.js",
    "lint": "eslint .",
    "test": "cross-env NODE_ENV=test jest --verbose --runInBand --forceExit",
    "start:test": "cross-env NODE_ENV=test node index.js",
    "docker-start": "docker compose -f docker-compose.dev.yml up",
    "docker-close": "docker compose -f docker-compose.dev.yml down --volumes"
  },
  "author": "MisterOz93",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^8.20.0",
    "jest": "^28.1.3",
    "nodemon": "^2.0.19"
  },
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "express-async-errors": "^3.1.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.5.0",
    "supertest": "^6.2.4"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
